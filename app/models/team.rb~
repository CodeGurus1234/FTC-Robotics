class Team < ActiveRecord::Base
  # attr_accessible :title, :body
  def self.upload(file)
    data = SmarterCSV.process(file)    
    data.each do |team|	
	team[:date_registered]= Date.strptime("%m/%d/%Y", team[:date_registered])
	Team.create!(team)
    end
  end
end 
