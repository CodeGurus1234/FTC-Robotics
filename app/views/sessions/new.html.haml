%h1 Login

= form_tag login_create_path do

  = label :team, :team, 'Team Number'
  = text_field :team, 'team'

  = label :team, :email, 'E-MAIL'
  = text_field :team, 'email'

  = submit_tag 'Login to my account'
%br
  = link_to 'Sign-­up as a new team', new_team_path


