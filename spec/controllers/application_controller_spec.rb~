require 'spec_helper'

describe ApplicationController do
 describe "set_current_user" do
    it "sets the current users" do
      assigns(@current_user).should be_a_new(session[:session_token]&&
			 User.find_by_session_token(session[:session_token]))
    end
  end

end
